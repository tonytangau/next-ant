# Next.js with Ant Design example

A small example showing Next.js with [Ant Design](http://ant.design)

Install it and run:

```bash
npm install
npm run dev [-- -p <port>]
```

OR

```bash
yarn
yarn run dev [-- -p <port>]
```
